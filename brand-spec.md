# Diana Codex visual specification

## Positioning

Codex 原生工具感 × 安静的嘉然陪伴。角色是环境的一部分，不抢占任务内容，不模拟官方联名。

## Color tokens

| Token | Dark | Light |
|---|---:|---:|
| Surface | `#0D0C0F` | `#FBF8F6` |
| Panel | `#171419` | `#FFFFFF` |
| Ink | `#F3EEF0` | `#2C2529` |
| Muted | `#A9A1A7` | `#7E7178` |
| Accent | `#D86E91` | `#B84970` |
| Accent soft | `#38242D` | `#F2DCE3` |
| Border | `#2B262D` | `#E8DFE2` |

## Layout and character

- 首页：角色位于右下角，宽度约为内容区的 `15–18%`。
- 对话页：保持同一锚点，透明度降至约 `7–10%` 的视觉存在感。
- 日间使用原始暖色透明素材；夜间使用单独调色的灰紫、干燥玫瑰与暖灰米色素材，不以简单反色代替。
- 夜间人物最终显示亮度约为素材的 `70–75%`，避免皮肤、白色衣物和裙摆形成发光块。
- 夜间人物使用 `diana-night-v3.png`：透明边缘不得出现白色光圈，头顶呆毛、猫耳轮廓和右侧长发发梢必须完整；生产过程由 `tools/rebuild_night_alpha.py` 复现。
- 角色层必须 `pointer-events: none`，不得遮挡输入、滚动和按钮。
- 文字、代码、diff 与审批信息优先级永远高于装饰。

## Decorative line art

- 使用用户确认的 ImageGen 线稿样张作为唯一笔触基准：右上为星形发夹、卷发曲线与草莓垂饰，左下为蝴蝶结、草莓和稀疏星点。
- 生产资产从批准样张确定性提取为透明 PNG 遮罩，不再手工重描 SVG；提取过程由 `tools/extract_approved_line_art.py` 复现。
- PNG 遮罩保留铅笔线条本身约 `53%` 的平均 Alpha；CSS 层夜间使用 `16%`、日间使用 `22%`，最终有效视觉强度约为夜间 `8.5%`、日间 `11.5%`。
- 线稿停留在右上、左下等空白角落，不穿过正文、输入框、代码或 Diff。
- 装饰层必须 `pointer-events: none`，并与人物层分离，方便单独关闭或替换。
- 左上角使用独立的心形＋糖果细线素材，不再镜像右上角；构图必须同时沿上边和左边展开，形成柔和的 L 形四角关系，显示宽度约为工作区的 `50%`。
- 左上主线约 `70–80%` 保持连贯，仅在糖果、点阵和左侧两处留下克制的短断点；装饰不能退化成一排彼此无关的悬浮图标。
- 左上装饰的实际显示边界约为 `800 × 230 px`（随工作区等比缩放）；左侧短臂必须在该区域内结束，不得继续侵入左下叙事小人区域。
- 左上笔触应与右上批准线稿属于同一美术系统：极细主曲线、局部伴随线、少量回旋与标点式小点、选择性双描；禁止使用等粗长弧线串联大图标的简化画法。
- 右上角继续保留原有星形、卷线与草莓组合，左上素材调整不得影响右上资产或定位。

## Character-side ornaments

- 五角星以用户指定参考截图为准：空心、圆角、轻微不对称，使用灰粉与暖米色的双重手绘细线，不再使用纯色几何遮罩。
- 糖果只使用一颗包装糖与一颗棒棒糖，沿立绘两侧空白分散放置；不得覆盖人物面部、正文或输入框。
- 日间与夜间共用透明彩色位图，分别通过亮度、饱和度与透明度适配，不对正文文字做任何调色。
- 立绘旁可放置两只枝江吉祥物“阿草”线稿：抱爱心阿草位于裙摆左侧，应援棒阿草位于腿部右侧；二者使用左下叙事小人的细铅笔／粉笔线条，不使用厚重卡通描边或实体白色填充。

## Controls

- 常规圆角：`8px`
- 卡片圆角：`12px`
- 输入框圆角：`14px`
- 只对主按钮、选中态和焦点环使用强调色。
- 动效时长：`120–180ms`，并尊重 `prefers-reduced-motion`。

## Public showcase

- `preview/index.html` 是唯一公开展示页面，README 日夜静帧由该页面生成。
- 展示页使用固定 `1920 × 1080` 舞台，并在任意窗口中保持 16:9 等比缩放；16:9 输出不得产生额外黑边或裁切。
- 展示页不得读取或复刻真实用户任务，只允许使用虚构环境和本仓库公开文件名。
- 桌面骨架必须以实际 Codex Windows 客户端为比例基准：`34px` 系统标题栏、`48px` 线程栏、约 `21%` 宽的任务侧栏、普通消息字号、工具活动行、文件变更卡、环境信息面板和固定底部输入区；禁止用营销页标题或通用仪表盘卡片替代真实工作会话。
- 图标采用一致的细线桌面控件语言，系统菜单、窗口按钮和面板开关保留可识别含义；演示交互不得伪装成不可点击的装饰。
- 主题素材的位置、尺寸、滤镜和透明度必须跟随 `themes/diana-dark/theme.css` 与 `themes/diana-light/theme.css` 的定稿参数，不得另行美化造成宣传图与实际主题不一致。
- 动态演示使用“提出任务、执行中、完成”三个场景，提供播放、暂停和时间轴拖动，并尊重 `prefers-reduced-motion`。
